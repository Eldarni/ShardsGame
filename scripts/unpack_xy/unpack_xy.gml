/// unpack_xy(xy)
/// Unpacks the xy integer from "pack_xy(x, y)" and return an array like [x, y]
/// @param xy

//-----------------------------------------------------------------------------

return [(argument0 >> 16), (argument0 & 0xffff)];

//-----------------------------------------------------------------------------